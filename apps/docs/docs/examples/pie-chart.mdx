---
description: Pie charts examples with React and Google Charts
tags:
  - Pie Chart
sidebar_position: 2
---

import ContextProvider from "../../src/components/ContextProvider";

# Pie Chart

Start by importing the Chart component :

```jsx
import { Chart } from "react-google-charts";
```

```jsx live
function App() {
  const data = [
    ["Task", "Hours per Day"],
    ["Work", 9],
    ["Eat", 2],
    ["Commute", 2],
    ["Watch TV", 2],
    ["Sleep", 7],
  ];

  const options = {
    title: "My Daily Activities",
  };
  return (
    <Chart
      chartType="PieChart"
      data={data}
      options={options}
      width={"100%"}
      height={"400px"}
    />
  );
}
```

[![Edit chart example in CodeSandbox](https://codesandbox.io/static/img/play-codesandbox.svg)](https://codesandbox.io/p/sandbox/github/RakanNimer/react-google-charts/tree/master/sandboxes/pie-chart/default?file=%2FApp.tsx&from-embed=)

---

## High-Level Overview

To render a Pie Chart, you need to provide the following props to the `Chart` component:

- **`chartType`**: Set this to `"PieChart"` to specify the chart type.
- **`data`**: Provide the chart data in a tabular format. First row containing the column definitions and subsequent rows the data.
- **`options`**: (Optional) Customize the chart with various options like title, colors, and display settings.
- **`width`** and **`height`**: (Optional) Define the size of the chart.

### Basic Usage

```jsx
import React from "react";
import { Chart } from "react-google-charts";

const data = [
  ["Task", "Hours per Day"],
  ["Work", 11],
  ["Eat", 2],
  ["Commute", 2],
  ["Watch TV", 2],
  ["Sleep", 7],
];

// Optional
const options = {
  title: "My Daily Activities",
};

function App() {
  return <Chart chartType="PieChart" data={data} options={options} />;
}

export default App;
```

---

## Examples

Below are interactive examples of Pie Charts rendered using `react-google-charts`. Each example demonstrates different features and customization options. You can interact with the charts directly in your browser.

### 3D Pie Chart

This example demonstrates how to create a 3D Pie Chart.

```jsx live
function App() {
  const data = [
    ["Task", "Hours per Day"],
    ["Work", 11],
    ["Eat", 2],
    ["Commute", 2],
    ["Watch TV", 2],
    ["Sleep", 7],
  ];

  const options = {
    title: "My Daily Activities",
    pieHole: 0.4, // Creates a Donut Chart. Does not do anything when is3D is enabled
    is3D: true, // Enables 3D view
    // slices: {
    //   1: { offset: 0.2 }, // Explodes the second slice
    // },
    pieStartAngle: 100, // Rotates the chart
    sliceVisibilityThreshold: 0.02, // Hides slices smaller than 2%
    legend: {
      position: "bottom",
      alignment: "center",
      textStyle: {
        color: "#233238",
        fontSize: 14,
      },
    },
    colors: ["#8AD1C2", "#9F8AD1", "#D18A99", "#BCD18A", "#D1C28A"],
  };
  return (
    <Chart
      chartType="PieChart"
      data={data}
      options={options}
      width={"100%"}
      height={"400px"}
    />
  );
}
```

### Diff Pie Chart

This example shows how to create a Diff Pie Chart to compare two sets of data.

<ContextProvider>
  {({ branch, theme }) => (
    <iframe
      src={`https://codesandbox.io/embed/github/RakanNimer/react-google-charts/tree/${branch}/sandboxes/pie-chart/diff?fontsize=14&hidenavigation=1&module=%2FApp.tsx&theme=${theme}`}
      style={{
        width: "100%",
        height: "500px",
        border: 0,
        borderRadius: "4px",
        overflow: "hidden",
      }}
      title="RakanNimer/react-google-charts: Pie Chart Diff Pie Chart"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
      loading="lazy"
    ></iframe>
  )}
</ContextProvider>

### Donut Chart

You can create a Donut Chart by adding a hole to the center of the pie using the .

<ContextProvider>
  {({ branch, theme }) => (
    <iframe
      src={`https://codesandbox.io/embed/github/RakanNimer/react-google-charts/tree/${branch}/sandboxes/pie-chart/donut?fontsize=14&hidenavigation=1&module=%2FApp.tsx&theme=${theme}`}
      style={{
        width: "100%",
        height: "500px",
        border: 0,
        borderRadius: "4px",
        overflow: "hidden",
      }}
      title="RakanNimer/react-google-charts: Pie Chart Donut Chart"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
      loading="lazy"
    ></iframe>
  )}
</ContextProvider>

### Exploding a Slice

Learn how to "explode" or offset a slice from the pie chart for emphasis.

<ContextProvider>
  {({ branch, theme }) => (
    <iframe
      src={`https://codesandbox.io/embed/github/RakanNimer/react-google-charts/tree/${branch}/sandboxes/pie-chart/exploding?fontsize=14&hidenavigation=1&module=%2FApp.tsx&theme=${theme}`}
      style={{
        width: "100%",
        height: "500px",
        border: 0,
        borderRadius: "4px",
        overflow: "hidden",
      }}
      title="RakanNimer/react-google-charts: Pie Chart Exploding a Slice"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
      loading="lazy"
    ></iframe>
  )}
</ContextProvider>

### Removing Slices

This example demonstrates how to remove slices below a certain threshold from the pie chart.

<ContextProvider>
  {({ branch, theme }) => (
    <iframe
      src={`https://codesandbox.io/embed/github/RakanNimer/react-google-charts/tree/${branch}/sandboxes/pie-chart/rm-slice?fontsize=14&hidenavigation=1&module=%2FApp.tsx&theme=${theme}`}
      style={{
        width: "100%",
        height: "500px",
        border: 0,
        borderRadius: "4px",
        overflow: "hidden",
      }}
      title="RakanNimer/react-google-charts: Pie Chart Removing Slices"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
      loading="lazy"
    ></iframe>
  )}
</ContextProvider>

### Rotating a Pie Chart

Learn how to rotate the pie chart to start from a different angle.

<ContextProvider>
  {({ branch, theme }) => (
    <iframe
      src={`https://codesandbox.io/embed/github/RakanNimer/react-google-charts/tree/${branch}/sandboxes/pie-chart/rotation?fontsize=14&hidenavigation=1&module=%2FApp.tsx&theme=${theme}`}
      style={{
        width: "100%",
        height: "500px",
        border: 0,
        borderRadius: "4px",
        overflow: "hidden",
      }}
      title="RakanNimer/react-google-charts: Pie Chart Rotating a Pie Chart"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
      loading="lazy"
    ></iframe>
  )}
</ContextProvider>

### Slice Visibility Threshold

This example shows how to set a visibility threshold to group smaller slices into an "Other" category.

<ContextProvider>
  {({ branch, theme }) => (
    <iframe
      src={`https://codesandbox.io/embed/github/RakanNimer/react-google-charts/tree/${branch}/sandboxes/pie-chart/threshold?fontsize=14&hidenavigation=1&module=%2FApp.tsx&theme=${theme}`}
      style={{
        width: "100%",
        height: "500px",
        border: 0,
        borderRadius: "4px",
        overflow: "hidden",
      }}
      title="RakanNimer/react-google-charts: Pie Chart Slice Visibility Threshold"
      allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
      sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
      loading="lazy"
    ></iframe>
  )}
</ContextProvider>

---

## Customizing the Pie Chart

You can customize various aspects of the Pie Chart using the `options` prop.

### Styling Options

Customize the chart's appearance:

```jsx
const options = {
  title: "My Daily Activities",
  is3D: true, // Enables 3D view
  pieHole: 0.4, // Will be ignored if is3D is set to true
  slices: {
    1: { offset: 0.2 }, // Explodes the second slice
  },
  pieStartAngle: 10, // Rotates the chart
  sliceVisibilityThreshold: 0.02, // Hides slices smaller than 2%
  legend: {
    position: "bottom",
    alignment: "center",
  },
  colors: ["#AFCBFF", "#C2E0C6", "#D8A7B1", "#FFC107", "#D3D3D3"],
};
```

### Exploding a Slice

To emphasize a particular slice, you can "explode" it by setting an offset:

```jsx
const options = {
  slices: {
    2: { offset: 0.2 }, // Explodes the third slice
  },
};
```

### Rotating the Pie Chart

Adjust the starting angle of the pie chart:

```jsx
const options = {
  pieStartAngle: 135, // Rotates the chart by 135 degrees
};
```

### Slice Visibility Threshold

Group smaller slices into an "Other" category:

```jsx
const options = {
  sliceVisibilityThreshold: 0.05, // Slices smaller than 5% will be grouped
};
```

---

## Additional Resources

- [Data Format](https://developers.google.com/chart/interactive/docs/gallery/piechart#data-format)
- [Configuration Options](https://developers.google.com/chart/interactive/docs/gallery/piechart#configuration-options)
- [Reference](https://developers.google.com/chart/interactive/docs/gallery/piechart)
- [Diff Charts Data Format](https://developers.google.com/chart/interactive/docs/gallery/diffchart#data-format)
- [Diff Charts Reference](https://developers.google.com/chart/interactive/docs/gallery/diffchart)

---
