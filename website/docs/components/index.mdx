---
slug: /components
description: List of react-google-charts components.
---

import Link from '@docusaurus/Link';
import { docs } from './docs';

# Components

<ul>
  {docs.map(({ title, slug }, i) => (
    <li key={i}>
      <Link to={slug}>{title}</Link>
    </li>
  ))}
</ul>
